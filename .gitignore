# Node modules (ไฟล์ไลบรารีที่ Vercel จะโหลดให้ใหม่เอง)
node_modules/

# Environment variables (เก็บรหัสลับ ห้ามเอาขึ้น GitHub เด็ดขาด!)
.env
.env.local
.env.development
.env.test
.env.production

# Mac OS hidden files (สำหรับคนใช้ Mac)
.DS_Store

# Logs (ไฟล์ประวัติการทำงาน)
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vercel specific
.vercel/